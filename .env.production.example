# ============================================
# ProjectScaffolder - Production Environment
# ============================================
# This file is for production deployment configuration
# Copy values to your hosting platform's environment variable settings
# For Vercel: Project Settings > Environment Variables

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# Production PostgreSQL connection string with SSL
# Use connection pooling for serverless environments
DATABASE_URL="postgresql://postgres:[password]@production-host:5432/projectscaffolder?sslmode=require"

# For Supabase with connection pooling:
# DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# --------------------------------------------
# NextAuth Configuration
# --------------------------------------------
# Production application URL
NEXTAUTH_URL="https://your-domain.com"

# Strong secret key for production (MUST be different from development)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="CHANGE-THIS-TO-A-STRONG-RANDOM-SECRET-IN-PRODUCTION"

# --------------------------------------------
# AI Provider API Keys
# --------------------------------------------
# Production API keys (separate from development keys for security)

# Anthropic Claude
ANTHROPIC_API_KEY="sk-ant-api03-..."

# OpenAI
OPENAI_API_KEY="sk-..."

# Google AI
GOOGLE_AI_API_KEY="..."

# Default provider
DEFAULT_LLM_PROVIDER="anthropic"

# --------------------------------------------
# OAuth Providers
# --------------------------------------------
# Production OAuth credentials

# GitHub OAuth
# Callback URL: https://your-domain.com/api/auth/callback/github
GITHUB_ID="production-github-client-id"
GITHUB_SECRET="production-github-client-secret"

# Google OAuth
# Callback URL: https://your-domain.com/api/auth/callback/google
GOOGLE_CLIENT_ID="production-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="production-google-client-secret"

# --------------------------------------------
# Deployment Integration
# --------------------------------------------
# Production GitHub token with appropriate permissions
GITHUB_TOKEN="ghp_production_token"

# Production Vercel token
VERCEL_TOKEN="production_vercel_token"

# --------------------------------------------
# Application Settings
# --------------------------------------------
NODE_ENV="production"

# --------------------------------------------
# Supabase Production (if using)
# --------------------------------------------
# NEXT_PUBLIC_SUPABASE_URL="https://[production-project-ref].supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="production-anon-key"
# SUPABASE_SERVICE_ROLE_KEY="production-service-role-key"

# --------------------------------------------
# Security & Monitoring (Optional)
# --------------------------------------------
# Sentry DSN for error tracking
# SENTRY_DSN="https://...@sentry.io/..."

# Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-..."

# Rate limiting (if using external service)
# UPSTASH_REDIS_REST_URL="https://..."
# UPSTASH_REDIS_REST_TOKEN="..."

# --------------------------------------------
# IMPORTANT PRODUCTION CHECKLIST
# --------------------------------------------
# Before going live, ensure:
# [ ] All secrets are unique and strong (not copied from examples)
# [ ] DATABASE_URL uses SSL/TLS connection
# [ ] NEXTAUTH_SECRET is cryptographically secure
# [ ] OAuth redirect URLs match your production domain
# [ ] API keys are from production accounts (not test/dev keys)
# [ ] Enable SSL enforcement in database
# [ ] Set up database backups
# [ ] Configure monitoring and alerting
# [ ] Review and enable rate limiting
# [ ] Enable CORS only for your domain
# [ ] Configure Content Security Policy (CSP)
